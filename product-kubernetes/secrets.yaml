apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
  namespace: analytics
  annotations:
    managed-by: "vault-agent-injector-or-external-secrets"
type: Opaque
stringData:
  DB_USERNAME: "REPLACE_AT_DEPLOY_TIME"
  DB_PASSWORD: "REPLACE_AT_DEPLOY_TIME"
  DB_URL: "REPLACE_AT_DEPLOY_TIME"
  JWT_SIGNING_KEY: "REPLACE_AT_DEPLOY_TIME"
