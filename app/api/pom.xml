<?xml version="1.0" encoding="UTF-8"?>
<!--
  Minimal stub for the analytics-api service.

  This repo (Automated-Enterprise-Deployment-Platform) is the deployment
  backbone only - it intentionally ships no application source. The
  Jenkinsfile's multi-repo checkout expects a real "analytics-app" repo to
  supply this. Since that repo doesn't exist for this capstone, this is a
  minimal, honest stand-in - just enough to satisfy the contracts already
  defined in product-kubernetes/deployment.yaml (liveness/readiness probes,
  Prometheus scrape annotation) and product-docker/Dockerfile (a jar named
  analytics-api-*.jar, an application-prod.yml to copy into /app/config).
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>2.7.18</version>
    <relativePath/>
  </parent>

  <groupId>com.skillfyme.analytics</groupId>
  <artifactId>analytics-api</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <properties>
    <java.version>11</java.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-registry-prometheus</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>analytics-api-${project.version}</finalName>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
